{
  "name": "freeplay-functions",
  "description": "Cloud Functions for the Freeplay NL→DDAR translateStep endpoint.",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "test": "vitest run",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.0.1",
    "openai": "^4.67.3"
  },
  "devDependencies": {
    "@types/node": "^20.16.0",
    "typescript": "^5.9.3",
    "vitest": "^2.1.9"
  }
}
